# Replay Environment Template
# Project-specific env variables are defined per package.
# Copy client/.env.example -> client/.env
# Copy server/.env.example -> server/.env
# Populate both using the secret inventory documented in
# config/push-notifications/README.md.

# Shared configuration (optional)
PORT=3001
